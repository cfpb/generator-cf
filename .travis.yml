language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-cf' ]; then cd .. && eval "mv $currentfolder generator-cf" && cd generator-cf; fi

